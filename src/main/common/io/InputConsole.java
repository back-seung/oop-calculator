package main.common.io;import main.calculator.util.Menu;import main.calculator.model.CalculateExpression;import java.util.Scanner;public class InputConsole {    private final Scanner scanner = new Scanner(System.in);    // 반환 String    public Menu readMenu() {        return Menu.chooseMenu(scanner.nextLine());    }    // 그냥 String 반환    public CalculateExpression readExpression() {        return new CalculateExpression(scanner.nextLine());    }}