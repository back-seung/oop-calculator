package main.calculator.controller;import main.calculator.util.Menu;import main.calculator.service.CalculatorService;import main.common.io.InputConsole;import main.common.io.OutputConsole;import main.calculator.model.CalculateExpression;import main.calculator.model.Result;public class CalculatorController {    private final InputConsole inputConsole;    private final OutputConsole outputConsole;    private final CalculatorService calculatorService;    public CalculatorController(InputConsole inputConsole, OutputConsole outputConsole, CalculatorService calculatorService) {        this.inputConsole = inputConsole;        this.outputConsole = outputConsole;        this.calculatorService = calculatorService;    }    public void run() {        while (true) {            outputConsole.printMenu();            // String            Menu selectMenu = inputConsole.readMenu();            switch (selectMenu) {                case HISTORY -> {                    // TODO: Calculator(Service)가 Storage에서 History를 꺼내오는 것 구현                    outputConsole.printHistory(calculatorService.getHistory());                }                case CALCULATE -> {                    // TODO: Calculator(Service)가 Expression을 받고 결과를 만드는 것, Storage에 저장하는 것 구현                    // 반환을 String                    // ExpressionRequest expression = new ExpressionRequest(String expression);                    // 검증?                    CalculateExpression expression = inputConsole.readExpression();                    // Request 전달                    Result result = calculatorService.calculate(expression);                    outputConsole.printResult(result);                }                case EXIT -> {                    System.exit(0);                }            }        }    }}