package main.calculator.util;import main.common.exception.InvalidMenuOptionException;import java.util.Arrays;public enum Menu {    HISTORY("1", "조회"),    CALCULATE("2", "계산"),    EXIT("3", "종료");    private final String option;    private final String description;    Menu(String option, String description) {        this.option = option;        this.description = description;    }    public String getOption() {        return option;    }    public String getDescription() {        return description;    }    // from 으로 메서드명 변경    public static Menu chooseMenu(String inputOption) {        return Arrays.stream(Menu.values())                .filter(menu -> menu.option.equals(inputOption))                .findAny()                .orElseThrow(InvalidMenuOptionException::new);    }}