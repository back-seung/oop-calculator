package main.calculator.service;import main.calculator.engine.Calculator;import main.calculator.model.CalculateExpression;import main.calculator.model.Result;import main.calculator.repository.Storage;import java.util.List;public class CalculatorService {    private final Storage storage;    private final Calculator calculator;    public CalculatorService(Storage storage, Calculator calculator) {        this.storage = storage;        this.calculator = calculator;    }    // 하나의 메서드에는 하나의 책임만 지닌다.    public Result calculate(CalculateExpression calculateExpression) {        double answer = calculator.calculate(calculateExpression);        Result result = new Result(calculateExpression, answer);        storage.save(result);        return result;    }    public List<Result> getHistory() {        return storage.findAll();    }}